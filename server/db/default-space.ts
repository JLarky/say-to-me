/** Deterministic identity for the one-time Default space seeded by drizzle/0022. */
export const DEFAULT_SPACE_ID = "space-default";
export const DEFAULT_SPACE_NAME = "Default";
export const DEFAULT_SPACE_CONTEXT =
  "Your first space for repositories, worktrees, and agent sessions.";
