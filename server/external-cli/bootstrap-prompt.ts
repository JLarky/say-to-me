/**
 * Shared create-time bootstrap prompt for external CLI providers.
 * Keep short so bootstrap is cheap; not provider-specific.
 */
export const EXTERNAL_CLI_BOOTSTRAP_PROMPT = "don't think, just reply okay";
