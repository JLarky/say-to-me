# Jarvis Session

This workspace was initialized from `templates/jarvis` in the Say To Me repo.

Before starting work:

- Read the repo-level `AGENTS.md` and `README.md` when present.
- Read `sessions.md`, `tasks.md`, and `artifacts.md` in this workspace.
- Read the current task and any notes the user provides.
- Do not modify files until the user gives the next instruction.

Keep this workspace as the local coordination record for the Jarvis session:

- Use `sessions.md` for Say To Me and OpenCode session identifiers.
- Use `tasks.md` for active, blocked, and completed work items.
- Use `artifacts.md` for branches, pull requests, important files, screenshots, logs, and other outputs.

If this file looks stale, compare it with the canonical template at `templates/jarvis/AGENTS.md` in the Say To Me repo.
